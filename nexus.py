#Main script.

#Needs: 

#Instantiate a listener and a dmxworker.
#IO prep for any LVD stuff, status lights, etc.

#Method to update an array of queues using another array of queues.
    #The 'patch' array needs to populated with queue objects that have
    #priority information and delay information.
    #Each queue is essentially a channel.

#Method to hydrate a queue for a channel based on a sensor hit

#Loop to wrangle the above methods?

#Sensor update -> 
#update live sensor grid -> 
#loop to cause the live sensor grid to decay ->
#same loop tosses new items onto our channel queues